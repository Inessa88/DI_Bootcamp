-- Instructions
-- In this exercise we will be using the actors table from todays lesson.
-- 1. Count how many actors are in the table.

SELECT count(*) FROM actors;

-- 2. Try to add a new actor with some blank fields. What do you think the outcome will be ? - There is gonna be an error 'ERROR:  null value in column "date_birth" of relation "actors" violates not-null constraint'.
INSERT INTO actors (first_name, last_name, number_oscars) 
VALUES ('Matt', 'Damon', 2);
